[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "16"  # Используйте Node.js 16 на Netlify

# Или для максимальной совместимости
[context.production.environment]
  NODE_VERSION = "14"
  NODE_OPTIONS = "--openssl-legacy-provider"
